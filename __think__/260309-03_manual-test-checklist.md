# 手動テストチェックリスト (2026-03-09)

対象コミット: `bbc72a2`, `9b258c1`

---

## 1. 認証

- [x] Google OAuth ログイン → ルーム一覧表示
- [x] ゲストログイン → ルーム作成が制限される
- [x] ログアウト → ログイン画面に戻る
- [x] リロード後もログイン状態が維持される

---

## 2. ルーム管理

- [x] ルーム作成 → 一覧に表示
- [x] ルーム入室 → シーンエディタが開く（※新規ルームのローディング無限バグは修正済み）
- [x] ルーム削除 → 一覧から消える
- [ ] 別ユーザーのルームIDで直アクセス → 403 Forbidden（今回修正）

---

## 3. シーン

- [x] シーン作成（+ボタン）
- [ ] シーン名編集
- [x] シーン切替 → 背景・オブジェクトが切り替わる
- [ ] シーン切替直前に入力 → 内容が保存される（デバウンス flush 修正確認）
- [x] シーン削除 → オブジェクトも消える
- [ ] シーン並び替え（ドラッグ）
- [ ] シーン複製 → BGM・オブジェクトも複製される

---

## 4. オブジェクト

- [ ] オブジェクト追加（画像選択）
- [ ] ドラッグで移動
- [ ] リサイズ
- [ ] オブジェクト削除
- [ ] 複数選択 → 一括削除（bg/fg は除外される）

---

## 5. レイヤー管理

- [x] レイヤーパネルでオブジェクト一覧が表示
- [x] visible トグル → 表示/非表示が切り替わる
- [ ] opacity スライダー → 透明度が変わる
- [ ] レイヤー並び替え（ドラッグ）→ 描画順が変わる
- [ ] 連続で素早くドラッグしても順序が壊れない（今回修正）

---

## 6. BGM

- [ ] BGM トラック追加
- [x] BGM 再生・一時停止・停止（※音が実際に鳴ってるかは手動で要確認）
- [ ] 音量調整
- [ ] シーンへの BGM 割り当て
- [ ] シーン切替 → BGM がフェードイン/アウトで切り替わる
- [ ] 高速シーン切替 → BGM が壊れない（タイマー cleanup 修正確認）
- [ ] BGM をシーンから除去 → 全シーンから外れたら自動削除される（今回修正）

---

## 7. カットイン

- [ ] カットイン作成
- [ ] カットイン再生 → アニメーション表示
- [ ] カットイン停止

---

## 8. キャラクター

- [ ] キャラクター作成
- [ ] キャラクター編集（名前、画像）
- [ ] キャラクター削除

---

## 9. チャット

- [x] メッセージ送信 → 表示される
- [ ] リロード → 過去メッセージが復元（IndexedDB）
- [ ] 大量メッセージ → loadMore で追加読み込み

---

## 10. シナリオテキスト

- [ ] テキスト作成
- [ ] テキスト編集
- [ ] テキスト削除
- [ ] 表示/非表示切り替え

---

## 11. スナップショット保存/復元

- [ ] 30秒待機 → コンソールやネットワークタブで PUT /snapshot 確認
- [ ] タブを閉じる → beforeunload で保存（ネットワークタブで確認）
- [ ] ルーム再入室 → 前回の状態が復元される（シーン、オブジェクト、BGM等）
- [ ] 復元後に room.name, room.dice_system が正常に表示される（今回修正）

---

## 12. アセット

- [ ] アセットライブラリ表示 → 既存アセットが見える
- [ ] 画像アセットアップロード → ライブラリに反映
- [ ] オーディオアセットアップロード → ライブラリに反映
- [ ] アセット削除 → R2 + D1 から削除される
- [ ] アップロード後のアセットをオブジェクトとして配置できる

---

## 13. アニメーション画像

- [ ] GIF/WebP/APNG をオブジェクトとして配置 → アニメーション再生
- [ ] シーン切替後もアニメーションが継続

---

## 今回の修正に特化した確認項目

### セキュリティ (bbc72a2)
- [ ] ブラウザコンソールに `import.meta.env` が出力されない
- [ ] 別ユーザーの `GET /rooms/:id` → 403
- [ ] 別ユーザーの `GET /rooms/:id/snapshot` → 403
- [ ] ゲストの `GET /rooms/:id` → 許可される

### スナップショット品質 (9b258c1)
- [ ] 復元後の room に id, name, dice_system が存在する
- [ ] 30秒自動保存が安定動作（タイマーリセットされない）
- [ ] 大量チャット（100件超）があるルームでタブを閉じても保存される

### アセット品質 (9b258c1)
- [ ] アップロードしたアセットが正常に削除できる（R2キー一致確認）
- [ ] アップロード中にネットワーク切断 → 孤立ファイルが残らない（可能なら）

### BGM (9b258c1)
- [ ] BGM を最後のシーンから除去 → トラック自体が消える

### レイヤー (9b258c1)
- [ ] 複数選択で bg/fg 含めて削除 → bg/fg は削除されず、他だけ削除

---

## 発見した新規バグ

- **`/file/` CORSエラー**: wrangler.toml には localhost:6100 設定済みだが、Cloudflare Dashboard で環境変数が上書きされてるか、リデプロイ未反映。Dashboard確認が必要
- **ダイスロール DiceBot モジュール未バンドル**: `/2d6` 送信時に `Module not found: ../../lib/bcdice/game_system/DiceBot`。ダイスシステム未設定時のフォールバック問題

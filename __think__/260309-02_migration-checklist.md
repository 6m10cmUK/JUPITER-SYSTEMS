# Firebase → Cloudflare 移行 確認事項チェックリスト

## 完了済み ✅

### 認証
- [x] Google OAuth ログイン（BroadcastChannel対応）
- [x] ゲストログイン
- [x] トークン保存・復元（localStorage JWT）
- [x] ログアウト
- [x] ゲストユーザーのルーム作成制限（フロント+Worker API両方）

### ルーム管理
- [x] ルーム一覧取得（GET /api/rooms）
- [x] ルーム作成（POST /api/rooms）
- [x] ルーム入室（GET /api/rooms/:id）
- [x] ルーム削除

### R2 ファイル
- [x] ファイルアップロード（POST /upload）— パスprefix修正済み
- [x] ファイル配信（GET /file/*）— 認証不要
- [x] ファイル削除（DELETE /delete）

### アセット管理
- [x] アセットメタデータ CRUD（D1 assets テーブル）
- [x] 旧Firebase UID のR2アセット14件をD1に移行完了

### Worker API
- [x] CORS（PUT追加済み）
- [x] 使用量制御（レートリミット）
- [x] ストレージ使用量チェック
- [x] 一時エンドポイント `/api/r2-list` 削除済み

---

## 要確認 🔍

### ルーム内機能（スナップショット経由のローカルステート）
- [ ] シーン作成・編集・削除・並び替え
- [ ] オブジェクト配置・移動・リサイズ・削除
- [ ] キャラクター管理
- [ ] チャット送受信
- [ ] BGM再生・管理
- [ ] カットイン再生・管理
- [ ] シナリオテキスト編集
- [ ] レイヤー管理（表示/非表示、並び替え）

### スナップショット保存/復元
- [ ] 30秒周期の自動保存（オーナーのみ）
- [ ] beforeunloadでの保存
- [ ] ルーム再入室時のスナップショット復元
- [ ] 初回入室時（スナップショットなし）のデフォルトシーン作成

### アセット関連
- [ ] アセットライブラリ表示（移行済みアセット14件が表示されるか）
- [ ] 新規アセットアップロード → ライブラリ反映
- [ ] アセット削除 → R2＋D1両方から削除
- [ ] BGM用オーディオアセットの再生

### マルチユーザー（WebRTC P2P）
- [ ] シグナリング（KV polling）
- [ ] P2Pデータ同期

### デプロイ
- [ ] Vercel環境変数の更新（VITE_R2_WORKER_URL等）
- [ ] index.htmlからeruda削除（開発用デバッグツール）
- [ ] 本番ビルド・デプロイ確認

### GCP設定
- [ ] Google OAuth同意画面のアプリ名を「JUPITER-SYSTEMS」に変更

---

## 既知の問題
- R2のキーが旧Firebase UID（`KXKnihzRFNPxW3EbIekdyv8F3v03`）のまま
  → アクセスはWorkerの `/file/` 経由なので問題なし（URLで直接参照）
  → 削除時はパスprefixチェックで `users/{uid}/` と `{uid}/` 両方許可済み
